<?php
class Config {
	const EVENT_YEAR_RANGE 	= '1'; 	// Range of year to feed events
	const EVENT_YEAR_BEFORE = true; // Feed events from year before ? (true/false)
	const EVENT_YEAR_AFTER 	= true; // Feed events from year after ? (true/false)
}